# ─────────────────────────────────────────────────────────────
#  Voice Studio — Python Dependencies
#  Covers both app_v1.py (Classic) and app_v2.py (Premium)
#  Install: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────

# ── Core: Speech Recognition ──────────────────────────────────
SpeechRecognition>=3.10.0       # STT engine (Google Speech API)
PyAudio>=0.2.14                 # Microphone access layer

# ── Core: Text to Speech ──────────────────────────────────────
pyttsx3>=2.90                   # Offline TTS (SAPI5 / espeak / nsss)

# ── Optional: Online TTS + MP3 export ────────────────────────
gTTS>=2.5.0                     # Google TTS — required for MP3 export
pygame>=2.5.2                   # MP3 playback for gTTS output

# ── Optional: Audio Processing ───────────────────────────────
pydub>=0.25.1                   # Audio format conversion / fallback playback

# ── Build: Packaging to EXE / binary ─────────────────────────
pyinstaller>=6.0.0              # Cross-platform app bundler
